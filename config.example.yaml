# Copy this file to ~/.dtrack/config.yaml and fill in your details.
#
#   mkdir -p ~/.dtrack
#   cp config.example.yaml ~/.dtrack/config.yaml
#   chmod 600 ~/.dtrack/config.yaml

url: https://dtrack.example.com
api-key: odt_xxxxxxxx_replace_me
